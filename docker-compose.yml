version: '3.8'

services:
  agentic-cti:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentic-cti
    restart: unless-stopped

    environment:
      # LLM Configuration
      - LLM_PROVIDER=${LLM_PROVIDER:-ollama}
      - LLM_MODEL=${LLM_MODEL:-llama3.2:latest}
      - LLM_BASE_URL=${LLM_BASE_URL:-http://ollama:11434}
      - LLM_TEMPERATURE=${LLM_TEMPERATURE:-0.3}
      - LLM_MAX_TOKENS=${LLM_MAX_TOKENS:-4096}

      # Email Configuration
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT:-587}
      - SMTP_USE_TLS=${SMTP_USE_TLS:-true}
      - SMTP_USERNAME=${SMTP_USERNAME}
      - SMTP_PASSWORD=${SMTP_PASSWORD}
      - EMAIL_FROM=${EMAIL_FROM}
      - EMAIL_TO=${EMAIL_TO}
      - EMAIL_DAILY_TIME=${EMAIL_DAILY_TIME:-07:00}
      - EMAIL_TIMEZONE=${EMAIL_TIMEZONE:-America/Montreal}

      # STIX Export
      - ENABLE_STIX_EXPORT=${ENABLE_STIX_EXPORT:-true}
      - STIX_EXPORT_PATH=/app/data/stix_exports

      # Trend Vision One
      - TREND_VISION_ONE_ENABLED=${TREND_VISION_ONE_ENABLED:-false}
      - TREND_VISION_ONE_API_KEY=${TREND_VISION_ONE_API_KEY}
      - TREND_VISION_ONE_REGION=${TREND_VISION_ONE_REGION:-us}

      # OpenCTI
      - OPENCTI_ENABLED=${OPENCTI_ENABLED:-false}
      - OPENCTI_URL=${OPENCTI_URL:-http://opencti:8080}
      - OPENCTI_API_KEY=${OPENCTI_API_KEY}

      # NG-TIP Platform Integration
      - NGTIP_ENABLED=${NGTIP_ENABLED:-true}
      - NGTIP_URL=${NGTIP_URL:-http://ngtip-api:8503}
      - NGTIP_API_KEY=${NGTIP_API_KEY}
      - NGTIP_SSL_VERIFY=${NGTIP_SSL_VERIFY:-false}

      # Streamlit UI
      - STREAMLIT_PORT=${STREAMLIT_PORT:-8501}
      - STREAMLIT_AUTH_ENABLED=${STREAMLIT_AUTH_ENABLED:-true}
      - STREAMLIT_USERNAME=${STREAMLIT_USERNAME:-admin}
      - STREAMLIT_PASSWORD=${STREAMLIT_PASSWORD:-changeme}

      # Agent Configuration
      - AGENT_RUN_INTERVAL=${AGENT_RUN_INTERVAL:-86400}
      - AGENT_MAX_SOURCES=${AGENT_MAX_SOURCES:-20}
      - AGENT_MAX_ARTICLES_PER_SOURCE=${AGENT_MAX_ARTICLES_PER_SOURCE:-10}

      # Logging
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - LOG_FILE=/app/logs/agentic_cti.log

      # Crawl4ai
      - CRAWL4AI_BASE_URL=${CRAWL4AI_BASE_URL:-http://crawl4ai:11235}
      - USE_CRAWL4AI=${USE_CRAWL4AI:-false}

    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config

    ports:
      - "${STREAMLIT_PORT:-8501}:8501"

    networks:
      - agentic-cti-network

    depends_on:
      - ollama
      - ngtip-api

    command: python main.py schedule

  # Streamlit UI (separate container for better resource management)
  agentic-cti-ui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentic-cti-ui
    restart: unless-stopped

    environment:
      - LLM_PROVIDER=${LLM_PROVIDER:-ollama}
      - LLM_MODEL=${LLM_MODEL:-llama3.2:latest}
      - LLM_BASE_URL=${LLM_BASE_URL:-http://ollama:11434}
      - STREAMLIT_AUTH_ENABLED=${STREAMLIT_AUTH_ENABLED:-true}
      - STREAMLIT_USERNAME=${STREAMLIT_USERNAME:-admin}
      - STREAMLIT_PASSWORD=${STREAMLIT_PASSWORD:-changeme}

    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config

    ports:
      - "8502:8501"

    networks:
      - agentic-cti-network

    command: python main.py ui

  # Ollama for local LLM inference
  ollama:
    image: ollama/ollama:latest
    container_name: agentic-cti-ollama
    restart: unless-stopped

    volumes:
      - ollama-data:/root/.ollama

    ports:
      - "11434:11434"

    networks:
      - agentic-cti-network

    # Optional: Pull model on startup
    # entrypoint: ["/bin/sh", "-c", "ollama serve & sleep 5 && ollama pull llama3.2:latest && wait"]

  # Crawl4ai for advanced web scraping with JavaScript support
  crawl4ai:
    image: unclecode/crawl4ai:latest
    container_name: agentic-cti-crawl4ai
    restart: unless-stopped

    ports:
      - "11235:11235"

    networks:
      - agentic-cti-network

    environment:
      - CRAWL4AI_API_TOKEN=${CRAWL4AI_API_TOKEN:-}

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11235/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # REST API for URL submission
  agentic-cti-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agentic-cti-api
    restart: unless-stopped

    environment:
      - LLM_PROVIDER=${LLM_PROVIDER:-ollama}
      - LLM_MODEL=${LLM_MODEL:-llama3.2:latest}
      - LLM_BASE_URL=${LLM_BASE_URL:-http://ollama:11434}
      - CRAWL4AI_BASE_URL=${CRAWL4AI_BASE_URL:-http://crawl4ai:11235}
      - API_RATE_LIMIT=${API_RATE_LIMIT:-100}
      - API_RATE_LIMIT_PERIOD=${API_RATE_LIMIT_PERIOD:-60}

    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
      - ./config:/app/config

    ports:
      - "${API_PORT:-8000}:8000"

    networks:
      - agentic-cti-network

    depends_on:
      - crawl4ai
      - ollama

    command: python -m api.main

  # NG-TIP Platform API Server
  ngtip-api:
    build:
      context: ./NG-TIP/cti_genai_platform
      dockerfile: ../../Dockerfile
    container_name: ngtip-api
    restart: unless-stopped

    environment:
      # LLM Configuration
      - LLM_PROVIDER=${LLM_PROVIDER:-ollama}
      - LLM_MODEL=${LLM_MODEL:-llama3.2}
      - LLM_BASE_URL=${LLM_BASE_URL:-http://ollama:11434}
      - LLM_API_KEY=${LLM_API_KEY}

      # Neo4j Configuration
      - NEO4J_URI=${NEO4J_URI:-bolt://neo4j:7687}
      - NEO4J_USER=${NEO4J_USER:-neo4j}
      - NEO4J_PASSWORD=${NEO4J_PASSWORD:-password}

      # MISP Configuration (optional)
      - MISP_URL=${MISP_URL}
      - MISP_API_KEY=${MISP_API_KEY}

      # OpenCTI Configuration (optional)
      - OPENCTI_URL=${OPENCTI_URL}
      - OPENCTI_TOKEN=${OPENCTI_TOKEN}

    volumes:
      - ./NG-TIP/cti_genai_platform:/app
      - ngtip-data:/app/data

    ports:
      - "8503:8503"

    networks:
      - agentic-cti-network

    depends_on:
      - ollama
      - neo4j

    command: python api_server.py

  # NG-TIP Streamlit UI
  ngtip-ui:
    build:
      context: ./NG-TIP/cti_genai_platform
      dockerfile: ../../Dockerfile
    container_name: ngtip-ui
    restart: unless-stopped

    environment:
      # LLM Configuration
      - LLM_PROVIDER=${LLM_PROVIDER:-ollama}
      - LLM_MODEL=${LLM_MODEL:-llama3.2}
      - LLM_BASE_URL=${LLM_BASE_URL:-http://ollama:11434}
      - LLM_API_KEY=${LLM_API_KEY}

      # Neo4j Configuration
      - NEO4J_URI=${NEO4J_URI:-bolt://neo4j:7687}
      - NEO4J_USER=${NEO4J_USER:-neo4j}
      - NEO4J_PASSWORD=${NEO4J_PASSWORD:-password}

    volumes:
      - ./NG-TIP/cti_genai_platform:/app
      - ngtip-data:/app/data

    ports:
      - "8504:8501"

    networks:
      - agentic-cti-network

    depends_on:
      - ngtip-api
      - neo4j

    command: streamlit run app.py --server.port 8501

  # Neo4j Graph Database for NG-TIP
  neo4j:
    image: neo4j:latest
    container_name: ngtip-neo4j
    restart: unless-stopped

    environment:
      - NEO4J_AUTH=neo4j/${NEO4J_PASSWORD:-password}
      - NEO4J_PLUGINS=["apoc", "graph-data-science"]
      - NEO4J_dbms_security_procedures_unrestricted=apoc.*,gds.*

    volumes:
      - neo4j-data:/data
      - neo4j-logs:/logs

    ports:
      - "7474:7474"  # HTTP
      - "7687:7687"  # Bolt

    networks:
      - agentic-cti-network

  # Optional: OpenCTI (uncomment to enable)
  # opencti:
  #   image: opencti/platform:latest
  #   container_name: agentic-cti-opencti
  #   restart: unless-stopped
  #   environment:
  #     - OPENCTI_ADMIN_EMAIL=admin@opencti.io
  #     - OPENCTI_ADMIN_PASSWORD=changeme
  #     - OPENCTI_ADMIN_TOKEN=${OPENCTI_API_KEY}
  #   ports:
  #     - "8080:8080"
  #   networks:
  #     - agentic-cti-network

networks:
  agentic-cti-network:
    driver: bridge

volumes:
  ollama-data:
    driver: local
  ngtip-data:
    driver: local
  neo4j-data:
    driver: local
  neo4j-logs:
    driver: local
